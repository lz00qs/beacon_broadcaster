import 'package:flutter/material.dart';

class BluetoothDisabledPage extends StatelessWidget {
  const BluetoothDisabledPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bluetooth is disabled.'),
    );
  }
}
