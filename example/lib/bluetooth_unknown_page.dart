import 'package:flutter/material.dart';

class BluetoothUnknownPage extends StatelessWidget {
  const BluetoothUnknownPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bluetooth is unknown.'),
    );
  }
}
